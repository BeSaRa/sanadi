import { DateFixDirective } from './date-fix.directive';

describe('DateFixDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFixDirective();
    expect(directive).toBeTruthy();
  });
});
