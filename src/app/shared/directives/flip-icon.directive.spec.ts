import { FlipIconDirective } from './flip-icon.directive';

describe('FlipIconDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipIconDirective();
    expect(directive).toBeTruthy();
  });
});
