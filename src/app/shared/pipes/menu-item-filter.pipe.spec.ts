import { MenuItemFilterPipe } from './menu-item-filter.pipe';

describe('MenuItemFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MenuItemFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
