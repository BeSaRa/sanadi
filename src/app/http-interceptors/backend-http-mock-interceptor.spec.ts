import { BackendHttpMockInterceptor } from './backend-http-mock-interceptor';

describe('BackendHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new BackendHttpMockInterceptor()).toBeTruthy();
  });
});
