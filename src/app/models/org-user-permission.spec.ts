import { OrgUserPermission } from './org-user-permission';

describe('OrgUserPermission', () => {
  it('should create an instance', () => {
    expect(new OrgUserPermission()).toBeTruthy();
  });
});
