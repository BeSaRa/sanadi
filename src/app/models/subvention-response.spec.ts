import { SubventionResponse } from './subvention-response';

describe('SubventionResponse', () => {
  it('should create an instance', () => {
    expect(new SubventionResponse()).toBeTruthy();
  });
});
