import { InternalDepartment } from './internal-department';

describe('InternalDepartment', () => {
  it('should create an instance', () => {
    expect(new InternalDepartment()).toBeTruthy();
  });
});
