import { OrgUserCustomRolePermission } from './org-user-custom-role-permission';

describe('OrgUserCustomRolePermission', () => {
  it('should create an instance', () => {
    expect(new OrgUserCustomRolePermission()).toBeTruthy();
  });
});
