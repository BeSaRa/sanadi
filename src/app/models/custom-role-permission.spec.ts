import { CustomRolePermission } from './custom-role-permission';

describe('CustomRolePermission', () => {
  it('should create an instance', () => {
    expect(new CustomRolePermission()).toBeTruthy();
  });
});
