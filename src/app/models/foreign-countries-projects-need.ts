import {SearchableCloneable} from '@app/models/searchable-cloneable';

export class ForeignCountriesProjectsNeed extends SearchableCloneable<ForeignCountriesProjectsNeed> {
  id!: number;
  projectName!: number;
}
