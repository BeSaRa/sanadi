import {IPredefinedDialogType} from './i-predefined-dialog-type';

export interface ITypeDialogList {
  alert: IPredefinedDialogType;
  info: IPredefinedDialogType;
  success: IPredefinedDialogType;
  error: IPredefinedDialogType;
  confirm: IPredefinedDialogType;
}
